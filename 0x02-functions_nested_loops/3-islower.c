#include "main.h"
/**
 * _islower - function checks if character is lower case or not
 * Return: 1 if true and 0 if false
 * @c: parameter is passed
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
