#include "main.h"
#include <stdio.h>
/**
 * _strlen - Returns length a string.
 * @str: string for length of.
 *
 * Return: length of @str.
 */

int _strlen(char *s)
{
	int l = sizeof(s) - 1;

	return (l);
}
