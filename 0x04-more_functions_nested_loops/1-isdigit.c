#include "main.h"
/**
 * _isdigit - function checks if character is lower case or not
 * Return: 1 if true and 0 if false
 * @c: parameter is passed
 */
int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
