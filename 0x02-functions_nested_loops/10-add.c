#include "main.h"
/**
 * add - function prints number's absolute
 * Return: the value
 * @x: parameter is passed
 * @y: parameter is passed
 * Description: xy is an local parameter
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
