#include "main.h"
/**
 * _isalpha - function checks if character is lower case or not
 * Return: 1 if true and 0 if false
 * @c: parameter is passed
 */
int _isalpha(int c)
{
	if (c >= 97 && c <= 122 && c >= 65 && c <= 90);
	{
		return (c >= 97 && c <= 122 || c >= 65 && c <= 90);
	}
}
